import 'package:flutter/material.dart';

class fourthpage extends StatelessWidget {
  const fourthpage({super.key});

  @override
  Widget build(BuildContext context) {
   return const Scaffold(body: Center(child: Text("fourthpage")),);
  }
}